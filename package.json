{
	"scripts": {
		"prepare": "husky install",
		"prettier": "prettier --write --ignore-unknown ."
	},
	"lint-staged": {
		"**/*": "prettier --write --ignore-unknown"
	},
	"devDependencies": {
		"husky": "^8.0.3",
		"lint-staged": "^14.0.1",
		"prettier": "^3.0.2"
	}
}
